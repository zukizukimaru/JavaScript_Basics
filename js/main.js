'use strict';

{
 
  // const name = 'wako'
  const name ='5'

  try {
    console.log(name.toUpperCase());
  } catch (e) {
    console,log(e);
  }

  console.log('Finish!');
}

//開発者が想定していなかった理由で起きるエラーを例外と言います。
